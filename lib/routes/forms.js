'use strict';

module.exports = [{
    method: 'GET',
    path: '/forms',
    options: {
        handler: async (request, h) => {}
    }
},
{
    method: 'GET',
    path: '/form/{id}',
    options: {
        handler: async (request, h) => {}
    }
}

];
